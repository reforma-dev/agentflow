# agentflow

**Agent loop:** [AGENTFLOW.md](AGENTFLOW.md) — grill → `/plan` → one chat per PR (implement → `/code-review` → `/commit` → `/handoff`).

This repo is the source of the loop and its skills. Consuming repos copy them; do not edit the copies.

Editing skills or this file → `writing-for-agents`.
